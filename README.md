# Simple Family Member V27

Upload every file in this folder to the root of the GitHub repository.

Files:
- `index.html` - application
- `api-config.js` - EPO OPS credentials used by the application
- `favicon.png` - browser icon
- `IP Aust_inline_white_95pxhigh.png` - header logo

Important: `api-config.js` is public when stored in a public GitHub repository. Moving the credentials out of `index.html` makes maintenance easier but does not secure them.
